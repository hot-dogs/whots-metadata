name: WHOTS Metadata

on:
  schedule:
    - cron: "0 0 1 * *" # 00:00 UTC on the 1st each month
  workflow_dispatch:
  push:
    paths-ignore:
      - ".github/.keepalive"
      - "results/items.json"

permissions:
  contents: write

concurrency:
  group: whots-metadata-${{ github.ref }}
  cancel-in-progress: false

jobs:
  scheduled:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      # Conda setup (avoid touching base; create a named env)
      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          miniforge-version: latest
          use-mamba: true
          auto-activate-base: false
          activate-environment: whots-${{ matrix.python-version }}
          python-version: ${{ matrix.python-version }}
          channels: conda-forge,defaults
          channel-priority: strict
          remove-profiles: true
          run-post: true

      # Remove problematic 'name:' or 'prefix:' lines from environment.yml
      - name: Sanitize environment.yml for CI
        shell: bash -l {0}
        run: |
          awk 'BEGIN{IGNORECASE=1} !/^[[:space:]]*name[[:space:]]*:/ && !/^[[:space:]]*prefix[[:space:]]*:/ {print}' environment.yml > environment_ci.yml
          echo "----- environment_ci.yml -----"
          cat environment_ci.yml

      - name: Create/Update env from sanitized file
        shell: bash -l {0}
        run: |
          mamba env update -n whots-${{ matrix.python-version }} -f environment_ci.yml
          conda info --envs
          which python
          python --version
          mamba list | head -n 50

      - name: Verify scrapy is available
        shell: bash -l {0}
        run: |
          which scrapy || true
          scrapy version

      - name: Fetch latest WHOTS data
        shell: bash -l {0}
        working-directory: ./src/whots_metadata
        run: |
          scrapy crawl whotsmetadata -O ../../results/items.json

      - name: Commit and push changes (with keepalive fallback)
        shell: bash -l {0}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

          git add -A
          if git diff --cached --quiet; then
            mkdir -p .github
            date -u +"%Y-%m-%dT%H:%M:%SZ" > .github/.keepalive
            git add .github/.keepalive
            git commit -m "[skip ci] keepalive: prevent schedule disable"
          else
            timestamp=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
            git commit -m "[skip ci] Latest WHOTS Metadata: ${timestamp}"
          fi
          git push
